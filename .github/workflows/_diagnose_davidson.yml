name: (temp) Diagnose Davidson County parcel service

# Temporary, workflow_dispatch-only diagnostic for the next parcel-
# expansion candidate: Davidson County TN / Nashville (34 facilities),
# the next uninvestigated county in the facility-count priority list
# after Multnomah County OR.
# Deleted once this is either added to the registry or documented as
# unavailable.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Probe Davidson County candidates
        run: node data/diagnose_davidson.mjs
