name: _diag_fairfax_dc_r1
on:
  workflow_dispatch:
jobs:
  diag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install pdf-parse@1.1.1
      - run: node data/_diag_fairfax_dc_r1.mjs
