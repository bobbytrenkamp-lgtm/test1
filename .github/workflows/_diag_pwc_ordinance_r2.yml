name: _diag_pwc_ordinance_r2
on:
  workflow_dispatch: {}

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install pdf-parse@1.1.1 --no-save
      - run: node data/_diag_pwc_ordinance_r2.mjs
